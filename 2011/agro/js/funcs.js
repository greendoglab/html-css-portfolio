$(document).ready(function() {
    $('.slider').cycle({
		fx: 'fade', // choose your transition type, ex: fade, scrollUp, shuffle, etc...
		prev:    '.slider_prev',
        next:    '.slider_next',
	});
});
